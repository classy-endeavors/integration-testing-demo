services:
  integration-tests:
    build:
      context: ../..
      dockerfile: integration-tests/Dockerfile
    environment:
      - SOCKSHOP_BASE_URL=http://edge-router
    depends_on:
      - edge-router
